# Chapter 6 — Vector Semantics & Embeddings

> J&M *Speech and Language Processing*, Ch. 6 (Vector Semantics and Embeddings).

Words as vectors: meaning from distribution. This chapter is the bridge from sparse counts to dense embeddings that power everything downstream.

## Exercises

- [`embeddings/`](embeddings/) — Explore pretrained word/sentence embeddings and similarity.
- [`how_embeddings_get_trained/`](how_embeddings_get_trained/) — Train your own embeddings with the skip-gram objective.
