# Copy to .env and fill in. .env is git-ignored.
# Used by the text-annotation capstone for hosted LLM access.
OPENAI_API_KEY=

# Optional: override the default model used for annotation.
# OPENAI_MODEL=gpt-4o-mini
